# LangChain (pinned <1.0.0 to block the langchain-classic fork which uses 1.x versioning)
langchain>=0.3.0,<1.0.0
langchain-community>=0.3.0,<0.4.0
langchain-text-splitters>=0.3.0,<1.0.0

# LLM provider (Groq — open source models, no cost)
langchain-groq

# Embeddings (local, no API key needed)
langchain-huggingface
sentence-transformers

# RAG evaluation framework
ragas
datasets

# Pinned to avoid source compilation on Python 3.11
numpy>=1.26.4,<2.0.0

# Utilities
python-dotenv
pydantic

# Jupyter kernel registration (run: python -m ipykernel install --user --name=langchain-eval)
ipykernel
